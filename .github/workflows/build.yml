name: Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '16.2'

      - name: Resolve package dependencies
        run: |
          xcodebuild -resolvePackageDependencies \
            -project Untouchable.xcodeproj \
            -scheme Untouchable \
            -quiet

      - name: Build (Release)
        run: |
          xcodebuild build \
            -project Untouchable.xcodeproj \
            -scheme Untouchable \
            -configuration Release \
            -derivedDataPath build \
            ONLY_ACTIVE_ARCH=NO \
            CODE_SIGN_IDENTITY="-" \
            CODE_SIGNING_ALLOWED=NO \
            -quiet

      - name: Verify build product exists
        run: test -d build/Build/Products/Release/Untouchable.app

      - name: Verify binary is valid Mach-O
        run: file build/Build/Products/Release/Untouchable.app/Contents/MacOS/Untouchable | grep -q "Mach-O"
